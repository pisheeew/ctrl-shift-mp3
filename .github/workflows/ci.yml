name: CI

# Every push to main and every pull request runs the unit suite, so broken
# commits never land unnoticed and external contributors get fast feedback.
# Read-only job: contents: read is the minimum the token needs to clone this
# private repo, and it grants nothing writable.
on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  test:
    name: Run the unit test suite
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up the pinned Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          # Matches the locally validated Python; bump deliberately.
          python-version: "3.14.3"

      - name: Install the pinned release dependencies
        # The same hash-pinned lockfile the release workflow installs: this
        # both exercises the cross-platform hashes on Linux and fails fast if
        # a lockfile entry loses its sha256. The notices test asserts the
        # generated third-party notices describe these exact versions.
        run: python -m pip install --require-hashes --requirement requirements-release.txt

      - name: Run the unit test suite
        run: python -m unittest tests.py --verbose
